#include <stdio.h>
#include <stdint.h>

#include "parse_common.h"
#include "parse_junit.h"

int LLVMFuzzerTestOneInput(const uint8_t * data, size_t size) {
    // parse_junit((const char *)data);
    return 0;
}
